rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow anyone to read (images are public)
      allow read: if true;
      // Allow writes for now (lock down later with auth if needed)
      allow write: if true;
    }
  }
}
